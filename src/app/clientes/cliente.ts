import { Injectable } from '@angular/core';
import { Producto } from '../productos/producto';

export class Cliente {
    id: string;
    nombre: string;
    direccion: string;
    telefono: string;
}
